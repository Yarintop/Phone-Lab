import 'package:flutter/material.dart';

class PartsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Parts Page"),
    );
  }
}
